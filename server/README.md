# Server Legacy Area

The active backend direction is `services/api`, using Python FastAPI with SQLite first.

This `server` directory preserves older server code under `legacy-dotnet` for reference and migration review. Do not add new FlowNote backend code here.
