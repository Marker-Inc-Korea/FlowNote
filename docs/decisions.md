# FlowNote 설계 결정 요약

이 문서는 기존 `docs/decisions/` 아래에 흩어져 있던 ADR을 현재 제품 방향 기준으로 합친 단일 결정 기록이다. 상충되는 내용은 최근 정리한 제품 방향, 현장 소리 반영 기준, 현장 관찰 기준을 따른다.

현재 코드와 비교할 때 이 문서는 구현 완료 목록이 아니라 제품 방향 결정이다. 실제 구현은 Windows WPF 로컬 SQLite 프로토타입과 FastAPI 헬스체크 골격이 기준이며, 서버 API, 보고서, AI, MES/ERP 연동 같은 미래 기능은 현재 구현 비교 대상이 아니다.

## 1. 제품 범위

- FlowNote는 생산공장 문서와 현장 지식을 함께 관리한다.
- 문서관리만 남기거나 순수 지식관리 시스템으로 확장하지 않는다.
- 초기 목표는 완성형 DMS/KMS가 아니라 문서, 버전, 현장 코멘트, 사진 기록, 작업순서, 작업내역을 축적하는 것이다.
- AI 검색, 작업 조언, 보고서 초안은 축적 데이터의 후속 활용 계층이다.

## 2. 기술 방향

- 백엔드는 Python FastAPI를 기준으로 한다.
- 클라이언트는 Windows WPF 기반 설치형 네이티브 앱 하나를 기준으로 한다.
- 운영 사용자는 일반 브라우저가 아니라 승인된 설치형 클라이언트로 접근한다.
- 메타데이터 DB는 SQLite 우선이며, 규모가 커지면 PostgreSQL로 확장한다.
- 파일은 서버 PC의 로컬 `storage/` 폴더에 저장한다.
- 배포는 서버 PC 1대와 클라이언트 설치파일을 기준으로 한다.

## 3. 문서와 파일

- 문서 메타데이터, 파일 바이너리, 문서 버전은 분리한다.
- 같은 문서의 수정은 기존 파일을 덮어쓰지 않고 새 버전으로 누적한다.
- 새 버전 등록 시 변경 사유를 기록한다.
- 업로드 문서를 항상 최신 확정본으로 보지 않고 작업중, 검토중, 공개, 보관 같은 상태를 둔다.
- PDF, 이미지, Excel, 텍스트는 우선 열람성을 높인다.
- CAD 원본 직접 뷰어와 CAD-PDF 자동 변환은 후속 검토 대상으로 둔다.

## 4. 문서 구조와 태그

- 문서 구조는 고객이 결정한다.
- 트리 구조나 현장 용어의 BOM 문서는 예시일 뿐 기본 강제 구조가 아니다.
- 작업지시 구조는 초기에는 관리자가 직접 입력한다.
- MES/ERP 연동이 추가되면 외부 원본 ID를 고객 정의 구조와 작업내역에 매핑한다.
- 문서 구조로 표현되지 않는 관계는 설비, 품목, 공정, 오류 유형, 라인, 위치 같은 태그로 보완한다.

## 5. 현장 코멘트와 보고서

- 현장 코멘트는 원천 이력으로 보존한다.
- 짧은 코멘트, 신호등식 기록, 정형 문구, 관리자 대리 입력을 지원한다.
- 사진 첨부와 작업일지 사진은 현장 기록의 원천 데이터로 본다.
- 문서 수정자, 열람자, 코멘트 등록자, 실제 전달자 또는 작업자 정보를 남긴다.
- 관리자 분석과 보고서화는 원천 코멘트를 실제 활용 가능한 정제 데이터로 바꾸는 중간 계층이다.
- 최종 보고서는 다시 문서로 저장하고 원천 코멘트와 작업내역을 추적한다.

## 6. 현장 의견 수용 기준

- 현장 의견은 수용, 불수용, 검토로 분리한다.
- 사진 기록, 짧은 코멘트, 인수인계, 작업순서판, 현장 공개 문서 열람은 초기 체감 기능으로 수용한다.
- 메신저 전체 기능, 개인 메신저 수집, GPS 추적, 근태 관리, 전체 MES/ERP 대체는 FlowNote 범위에서 제외한다.
- 개인 휴대폰 APK 설치와 iOS 앱은 현재 기본 배포 범위가 아니다.
- NFC 또는 사원카드 로그인, OCR, 음성 입력, 다국어, 근무시간 외 알림 제한은 후속 검토 대상으로 둔다.

## 7. 현장 관찰과 암묵지

- 현장 관찰은 요구사항 분석의 일부이다.
- 작업자의 감각 판단, 공정 중간 보류, 재작업, 선별, 금형 또는 원재료 상태에 대한 경험은 MES 정량 결과 뒤에 숨어 있는 현장지식 데이터로 본다.
- 이런 암묵지는 짧은 코멘트, 사진, 태그, 작업순서, 인수인계와 연결해 저장한다.
- 단말기 위치, OS, 사양, 통신 품질은 실증 전 확인해야 할 운영 조건이다.
- 현장 프론트는 단순해야 하며 클릭 수와 입력 부담을 줄인다.

## 8. MES/ERP와 외부 시스템

- FlowNote는 MES/ERP를 대체하지 않는다.
- MES/ERP는 작업지시, 생산실적, 설비, 품목, 공정 같은 정형 데이터의 원천이다.
- FlowNote는 문서, 현장 코멘트, 작업 문제점, 관리자 보고서, 작업내역을 연결해 정형 데이터가 설명하지 못하는 맥락을 보강한다.
- 외부 연동은 현장별 API, DB, 파일 연동 차이를 흡수하는 어댑터로 처리한다.
- 연동 실패와 동기화 이력을 남긴다.

## 9. 작업순서판

- 작업순서판은 MES 작업지시 원본을 대체하지 않는다.
- 사무실, 관리자, 반장, 조장이 조정한 실행 순서를 현장 TV 또는 클라이언트 화면으로 공유한다.
- 작업순서 변경, 상태 변경, 조회 이력은 감사 로그와 알림 이벤트로 남긴다.
- 기존 MES 작업지시 또는 생산순서 정보는 후속 어댑터로 참조할 수 있다.

## 10. 보안과 배포

- 현장 문서는 고객 자산이므로 사내 서버형 운영을 우선한다.
- 클라우드나 외부 접근은 기본값이 아니며 별도 협의가 필요한 후속 선택지이다.
- 로그인과 역할 기반 권한을 적용한다.
- 다운로드 차단과 뷰어 자동 닫힘은 Windows WPF 클라이언트와 서버 감사 로그를 함께 사용해 구현한다.
- FlowNote는 감시 도구가 아니며 개인 위치 추적이나 개인 메신저 수집을 하지 않는다.
