# 2026-06-23 작업 일지

## Windows WPF 탐색기형 화면

- 상태: 완료
- 범위: `apps/windows/src`에 신규 WPF 앱과 코어 프로젝트를 추가하고 탐색기형 셸 화면을 구현했다.
- 구현 내용:
  - 좌측 폴더 트리
  - 중앙 검색 입력과 문서 목록
  - 우측 문서 상세 패널
  - 상단 기본 작업 버튼 영역
  - 하단 상태 표시 영역
- 코드 위치:
  - `apps/windows/src/FlowNote.Windows.App/`
  - `apps/windows/src/FlowNote.Windows.Core/Explorer/`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개

## 문서 갱신

- `apps/windows/README.md`에 현재 Windows WPF 구현 상태를 추가했다.
- `apps/windows/docs/README.md`에 Windows 앱 구현 문서 목록을 정리했다.
- `apps/windows/docs/explorer-shell.md`에 탐색기형 셸 화면의 목적, 구조, 범위, 검증 결과를 기록했다.

## 로컬 SQLite 로그인, 문서 등록, 폴더 관리 기본 구조

- 상태: 완료
- 범위: Windows WPF 클라이언트의 로컬 SQLite 기반 기본 서비스와 로그인 선행 흐름을 추가했다.
- 구현 내용:
  - 앱 시작 시 로그인 창 표시
  - 로그인 성공 후에만 탐색기 화면 표시
  - 기본 계정 `admin / 1234` 자동 생성
  - 로컬 SQLite DB 초기화
  - 루트 폴더 자동 생성
  - 폴더 생성, 목록, 삭제 기본 서비스
  - 문서 등록, 목록 기본 서비스
  - WPF 화면의 새 폴더/문서 등록 버튼을 로컬 서비스에 연결
  - smoke test 프로젝트 추가
- 코드 위치:
  - `apps/windows/src/FlowNote.Windows.App/LoginWindow.xaml`
  - `apps/windows/src/FlowNote.Windows.App/MainWindow.xaml`
  - `apps/windows/src/FlowNote.Windows.Core/Auth/`
  - `apps/windows/src/FlowNote.Windows.Core/Storage/`
  - `apps/windows/src/FlowNote.Windows.Core/Folders/`
  - `apps/windows/src/FlowNote.Windows.Core/Documents/`
  - `apps/windows/src/FlowNote.Windows.SmokeTests/`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 문서 갱신: 로컬 SQLite

- `apps/windows/README.md`에 로그인, 로컬 SQLite, 기본 계정, 검증 명령을 반영했다.
- `apps/windows/docs/local-sqlite-basics.md`에 로컬 SQLite 구조와 검증 범위를 기록했다.
- `apps/windows/docs/explorer-shell.md`에 로그인과 로컬 SQLite 바인딩 범위를 반영했다.

## 좌측 탐색기 트리와 기본 폴더

- 상태: 완료
- 범위: 탐색기 좌측 영역에서 로컬 SQLite 폴더 구조를 트리로 표시하고, 루트 아래 기본 폴더를 시스템 폴더로 보장했다.
- 구현 내용:
  - `Root` 아래 기본 폴더 `문서`, `인수인계`, `작업순서`, `사진` 자동 생성
  - 네 개 기본 폴더를 삭제 불가 시스템 폴더로 처리
  - 기존 로컬 DB에 같은 이름의 기본 폴더가 있으면 시스템 폴더로 보정
  - 문서 등록 기본 대상을 `문서` 폴더로 변경
  - 스모크 테스트에 기본 폴더 존재 여부와 삭제 차단 검증 추가
- 코드 위치:
  - `apps/windows/src/FlowNote.Windows.Core/Storage/FlowNoteLocalDatabase.cs`
  - `apps/windows/src/FlowNote.Windows.Core/Folders/FolderService.cs`
  - `apps/windows/src/FlowNote.Windows.App/MainWindow.xaml.cs`
  - `apps/windows/src/FlowNote.Windows.SmokeTests/Program.cs`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 불필요 디렉터리 정리

- 상태: 완료
- 범위: 현재 개발 방향에서 실제 사용 근거가 없는 루트 디렉터리와 설명을 제거했다.
- 삭제 대상:
  - `LocalDev/`
  - `assets/`
  - `packages/`
  - `scripts/`
- 유지 대상:
  - `apps/windows/`
  - `services/api/`
  - `docs/`
- 문서 갱신:
  - `AGENTS.md`의 Repository Layout에서 삭제 대상 항목 제거
  - `README.md`의 저장소 구조에서 삭제 대상 항목 제거
  - `.gitignore`에서 `LocalDev/launchagents/` 항목 제거
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## WPF 화면 UI 조정

- 상태: 완료
- 범위: 기능 검증용 샘플 화면에 가까웠던 WPF UI를 현장 문서 관리용 탐색기 화면 톤으로 조정했다.
- 구현 내용:
  - 상단 영역을 제품명, 업무 설명, 로그인 사용자, 주요 명령으로 정리
  - 좌측 폴더 트리 헤더와 여백 조정
  - 오른쪽 파일 목록의 헤더, 검색 영역, 컬럼명을 한글 업무 화면 기준으로 정리
  - 로그인 창과 파일 보기 창의 문구와 모서리, 여백을 메인 화면과 맞춤
  - 공통 WPF 리소스에 버튼, 텍스트박스, 데이터그리드 기본 스타일 추가
  - 상태 메시지와 업로드 후보 상태 문구 한글화
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 실제 테스트 파일 기반 기능 테스트

- 상태: 완료
- 범위: 실제 테스트 파일을 로컬에 생성하고 파일 등록, 목록 조회, Drag & Drop 후보 추가 흐름을 확인했다.
- 테스트 위치:
  - `tmp/functional-flow-test-2026-06-23/`
- 남긴 테스트 산출물:
  - `tmp/functional-flow-test-2026-06-23/files/현장점검-2026-06-23.txt`
  - `tmp/functional-flow-test-2026-06-23/files/인수인계-주간.txt`
  - `tmp/functional-flow-test-2026-06-23/files/작업순서-라인A-조립검사.xlsx`
  - `tmp/functional-flow-test-2026-06-23/files/라인A-설비사진-2026-06-23.jpg`
  - `tmp/functional-flow-test-2026-06-23/flownote-functional-flow.sqlite`
  - `tmp/functional-flow-test-2026-06-23/functional-flow-result.md`
  - `tmp/functional-flow-test-2026-06-23/FunctionalFlowCheck.cs`
  - `tmp/functional-flow-test-2026-06-23/FunctionalFlowCheck.csproj`
- 테스트 계정:
  - ID: `jobhead`
  - 이름: `반장`
  - 비밀번호: `1234`
- SQLite 기록:
  - `user_accounts`에 `jobhead / 반장` 계정 기록
  - `documents`에 `반장` 작성자로 일반 문서, 인수인계 문서, 작업순서 문서, 사진 문서 기록
  - `document_folders`에 `/인수인계/2026-06-23`, `/사진/2026-06-23` 날짜 폴더 기록
- 확인한 흐름:
  - `jobhead / 1234` 로그인 성공
  - 실제 파일 4개 생성
  - 파일 등록 후 전체 문서 목록 조회 성공
  - 인수인계 파일이 날짜 하위 폴더에 배치됨
  - 사진 파일이 날짜 하위 폴더에 배치됨
  - 작업순서 파일 제목이 파일명 기반으로 자동 생성됨
  - Drag & Drop 후보가 파일 목록 최상단에 추가되고 사용자 이름 `반장`과 로컬 경로가 유지됨
- 결과:
  - 기능 테스트 통과
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 로그인 창 표시 문제 수정

- 상태: 완료
- 문제: 로그인 창 높이와 내부 배치가 부족해 비밀번호 입력 아래의 로그인 버튼이 화면에 보이지 않았다.
- 수정 내용:
  - `LoginWindow.xaml`의 창 높이와 최소 높이를 더 여유 있게 늘렸다.
  - 로그인 폼 내부를 고정 행 `Grid` 구조로 변경해 버튼 영역이 잘리지 않도록 했다.
  - 로그인 버튼 여백과 높이를 조정했다.
  - 비밀번호 입력칸의 세로 정렬을 맞췄다.
- 실제 확인:
  - `tmp/login-window-verify-fixed.png`로 실행 중인 `FlowNote 로그인` 창을 캡처했다.
  - 캡처 결과 로그인 버튼이 화면에 표시되는 것을 확인했다.
- 검증:
  - 최초 빌드는 실행 중인 `FlowNote.Windows.App` 프로세스가 exe를 잠가 실패했다.
  - 실행 중인 앱 프로세스를 종료한 뒤 재빌드했다.
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 탐색기 트리 아이콘 UI 반영

- 상태: 완료
- 범위: 왼쪽 폴더 트리를 Windows 탐색기와 비슷하게 아이콘과 트리 구조 중심으로 다시 표현했다.
- 수정 내용:
  - `ExplorerFolder`에 폴더 아이콘 표시용 glyph 속성 추가
  - TreeView 항목을 기본 펼침 상태로 설정
  - TreeView 항목 높이, 여백, 스크롤 설정 조정
  - 폴더명 앞에 `Segoe MDL2 Assets` 기반 폴더 아이콘 표시
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 문서 갱신: 기본 폴더 트리

- `apps/windows/README.md`에 루트 아래 기본 폴더 4개와 삭제 불가 정책을 반영했다.
- `apps/windows/docs/local-sqlite-basics.md`에 기본 폴더 자동 생성과 시스템 폴더 정책을 반영했다.
- `apps/windows/docs/explorer-shell.md`에 좌측 트리의 기본 폴더 구조를 반영했다.

## 오른쪽 파일 목록과 Drag & Drop 업로드 후보

- 상태: 완료
- 범위: 탐색기 오른쪽 작업 영역을 파일 목록 기본 화면으로 유지하고, 파일 목록에 Drag & Drop 업로드 후보를 추가하도록 구현했다.
- 구현 내용:
  - 오른쪽 작업 영역에 파일 목록 표시
  - 파일 목록에 파일명, 제목, 유형, 상태, 버전, 사용자, 버전 생성 시간 표시
  - 파일 목록 영역에 Drag & Drop 이벤트 연결
  - 드래그 진입, 이탈, 오버, 드롭 이벤트 처리
  - 드롭된 파일을 파일 목록의 업로드 후보 항목으로 추가
  - 업로드 후보에 로그인 사용자 이름과 후보 생성 시간 표시
  - 파일 목록 더블 클릭 시 별도 파일 보기 창 표시
  - 로컬 경로가 있는 작은 텍스트 파일은 보기 창에서 내용 표시
  - 업로드 후보 모델 `UploadCandidate` 추가
  - 파일 보기 창 `DocumentViewWindow` 추가
  - 스모크 테스트에 업로드 후보가 파일 목록에 추가되는지 검증 추가
- 코드 위치:
  - `apps/windows/src/FlowNote.Windows.App/MainWindow.xaml`
  - `apps/windows/src/FlowNote.Windows.App/MainWindow.xaml.cs`
  - `apps/windows/src/FlowNote.Windows.App/DocumentViewWindow.xaml`
  - `apps/windows/src/FlowNote.Windows.App/DocumentViewWindow.xaml.cs`
  - `apps/windows/src/FlowNote.Windows.Core/Explorer/UploadCandidate.cs`
  - `apps/windows/src/FlowNote.Windows.Core/Explorer/ExplorerWorkspace.cs`
  - `apps/windows/src/FlowNote.Windows.SmokeTests/Program.cs`
- 검증:
  - 최초 병렬 검증 중 `VBCSCompiler` 파일 잠금으로 WPF 빌드가 실패했다.
  - `dotnet build-server shutdown`으로 컴파일 서버 잠금을 해제한 뒤 재시도했다.
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 문서 갱신: 파일 목록과 Drag & Drop 업로드 후보

- `apps/windows/README.md`에 오른쪽 기본 화면이 파일 목록이라는 기준과 더블 클릭 보기 흐름을 반영했다.
- `apps/windows/docs/explorer-shell.md`에 파일 목록, Drag & Drop 후보 추가, 더블 클릭 보기 창 흐름을 반영했다.
- `apps/windows/docs/local-sqlite-basics.md`에 업로드 후보가 파일 목록에 추가되는 검증을 반영했다.
- 문서 갱신 후 `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`를 재실행했고 경고 0개, 오류 0개로 통과했다.
- 문서 갱신 후 `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`를 재실행했고 `FlowNote Windows smoke tests passed.` 결과를 확인했다.

## 기본 폴더별 등록 규칙 반영

- 상태: 완료
- 범위: `인수인계`, `작업순서`, `사진` 기본 폴더에 파일 등록 규칙을 추가했다.
- 구현 내용:
  - 기본 폴더 이름을 `FlowNoteLocalDatabase` 상수로 정리
  - `FolderService`에 기본 시스템 폴더 조회와 하위 폴더 없으면 생성 기능 추가
  - `DocumentPlacementService` 추가
  - `인수인계` 선택 시 `yyyy-MM-dd` 날짜 하위 폴더 생성 후 문서 배치
  - `사진` 선택 시 `yyyy-MM-dd` 날짜 하위 폴더 생성 후 문서 배치
  - `작업순서` 선택 시 파일명에서 확장자를 제외한 값을 작업 제목으로 자동 생성
  - 루트 선택 또는 폴더 미선택 시 기본 대상은 `문서` 폴더로 처리
  - WPF 트리 선택 상태를 문서 등록과 Drag & Drop 후보 추가 흐름에 연결
  - 스모크 테스트에 날짜 하위 폴더 생성과 작업 제목 자동 생성 검증 추가
- 문서 갱신:
  - `apps/windows/README.md`
  - `apps/windows/docs/explorer-shell.md`
  - `apps/windows/docs/local-sqlite-basics.md`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`
