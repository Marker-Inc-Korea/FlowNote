# 2026-06-23 작업 일지

일단 기존에 사용하든 client 앱에서 어느정도 현장에 검증된 부분을 가져와서 다시 만드는 작업을 합니다.
제 방법보다는 이런 방법을 도입해서 해야 하니 현장에 실제 사용되는것과 분리를 해야 하는 작업을 하는것이 더딜수 밖에 없습니다.

## Windows WPF 탐색기형 화면

- 상태: 완료
- 범위: `apps/windows/src`에 신규 WPF 앱과 코어 프로젝트를 추가하고 탐색기형 셸 화면을 구현했다.
- 구현 내용:
  - 좌측 폴더 트리
  - 중앙 검색 입력과 문서 목록
  - 우측 문서 상세 패널
  - 상단 기본 작업 버튼 영역
  - 하단 상태 표시 영역
- 코드 위치:
  - `apps/windows/src/FlowNote.Windows.App/`
  - `apps/windows/src/FlowNote.Windows.Core/Explorer/`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개

## 문서 갱신

- `apps/windows/README.md`에 현재 Windows WPF 구현 상태를 추가했다.
- `apps/windows/docs/README.md`에 Windows 앱 구현 문서 목록을 정리했다.
- `apps/windows/docs/explorer-shell.md`에 탐색기형 셸 화면의 목적, 구조, 범위, 검증 결과를 기록했다.

## 로컬 SQLite 로그인, 문서 등록, 폴더 관리 기본 구조

- 상태: 완료
- 범위: Windows WPF 클라이언트의 로컬 SQLite 기반 기본 서비스와 로그인 선행 흐름을 추가했다.
- 구현 내용:
  - 앱 시작 시 로그인 창 표시
  - 로그인 성공 후에만 탐색기 화면 표시
  - 기본 계정 `admin / 1234` 자동 생성
  - 로컬 SQLite DB 초기화
  - 루트 폴더 자동 생성
  - 폴더 생성, 목록, 삭제 기본 서비스
  - 문서 등록, 목록 기본 서비스
  - WPF 화면의 새 폴더/문서 등록 버튼을 로컬 서비스에 연결
  - smoke test 프로젝트 추가
- 코드 위치:
  - `apps/windows/src/FlowNote.Windows.App/LoginWindow.xaml`
  - `apps/windows/src/FlowNote.Windows.App/MainWindow.xaml`
  - `apps/windows/src/FlowNote.Windows.Core/Auth/`
  - `apps/windows/src/FlowNote.Windows.Core/Storage/`
  - `apps/windows/src/FlowNote.Windows.Core/Folders/`
  - `apps/windows/src/FlowNote.Windows.Core/Documents/`
  - `apps/windows/src/FlowNote.Windows.SmokeTests/`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`
  - 스모크 테스트에 업로드 파일의 로컬 경로가 `documents.local_path`와 `document_versions.local_path`에 저장되는 검증을 추가했다.

## 문서 폴더 하위 작업 분류 반영

- 상태: 완료
- 요청: `문서` 아래에 문서를 작업 성격별로 구분할 수 있는 폴더 구조를 둔다.
- 구현 내용:
  - `문서` 아래 기본 분류 폴더 `도면`, `작업표준서`, `점검표`, `품질검사`, `안전수칙`, `보전작업`, `일반문서`를 자동 생성하도록 했다.
  - 위 분류 폴더는 시스템 폴더로 처리하여 삭제할 수 없도록 했다.
  - `문서` 폴더에 파일을 등록하면 파일명과 제목 기준으로 분류 폴더에 자동 배치하도록 했다.
  - 기존 SQLite DB에서 `문서` 바로 아래에 있던 문서는 초기화 과정에서 분류 폴더로 이동하도록 했다.
  - 소스 시드 DB `apps/windows/src/FlowNote.Windows.App/Data/flownote.local.sqlite`도 같은 구조로 마이그레이션했다.
- 시드 DB 확인:
  - `문서` 하위 분류 폴더 7개 확인
  - `문서` 바로 아래 문서 0개 확인
  - `문서` 하위 분류 폴더로 이동된 문서 16개 확인
- 문서 갱신:
  - `apps/windows/README.md`
  - `apps/windows/docs/explorer-shell.md`
  - `apps/windows/docs/local-sqlite-basics.md`
- 검증:
  - 실제 WPF 앱 빌드 출력 경로 기준으로 소스 앱 디렉터리 `apps/windows/src/FlowNote.Windows.App`를 찾는 것을 확인했다.
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`
- 검증:
  - 최초 빌드에서 `DocumentPlacementService` 내부 메서드에 파일명과 제목을 전달하지 않은 컴파일 오류를 확인하고 수정했다.
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 파일 업로드 버튼과 PDF 파싱 미리보기

- 상태: 완료
- 요청: Drag & Drop 외에 파일 업로드 버튼을 만들고, 실제 PDF 파일을 파싱해서 보기 창에 표시한다.
- 구현 내용:
  - 메인 화면 상단에 `파일 업로드` 버튼을 추가했다.
  - 파일 선택 대화상자에서 여러 파일을 선택해 SQLite 문서로 즉시 저장할 수 있게 했다.
  - 파일 업로드 버튼과 Drag & Drop이 같은 DB 저장 흐름을 사용하도록 정리했다.
  - 업로드 파일은 앱 실행 폴더의 `Data\Files\Uploads\yyyy-MM-dd\` 아래로 복사하고 `documents`, `document_versions`에 원본 버전 `v1`로 기록하도록 수정했다.
  - PDF 미리보기는 `PdfPig` 패키지 기반 텍스트 파싱을 우선 사용하도록 변경했다.
  - PdfPig 결과가 샘플 PDF 인코딩 문제로 깨져 보이면 기존 단순 PDF 문자열 추출 방식으로 보정한다.
- 확인한 문제와 조치:
  - 최초 `UglyToad.PdfPig` 1.7.0 패키지 참조는 NuGet에서 찾을 수 없어 빌드가 실패했다.
  - NuGet 조회 결과 실제 사용 가능한 패키지 ID `PdfPig` 0.1.14로 변경했다.
- 테스트 산출물:
  - `tmp/pdf-preview-parse-test-2026-06-23/`
- 문서 갱신:
  - `apps/windows/README.md`
  - `apps/windows/docs/explorer-shell.md`
  - `apps/windows/docs/local-sqlite-basics.md`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`
  - `dotnet run --project .\tmp\pdf-preview-parse-test-2026-06-23\PdfPreviewParseTest.csproj -- "C:\Projects\Project\FlowNote\apps\windows\src\FlowNote.Windows.App\Data\Files\Samples\문서-작업표준서-혼합공정.pdf"`
  - 결과: PDF 파일에서 텍스트가 추출되는 것을 확인했다.

## PDF 원본 레이아웃 보기 반영

- 상태: 완료
- 요청: PDF 보기가 문서 그대로 보이지 않고 텍스트만 보이는 문제를 수정한다.
- 구현 내용:
  - 파일 보기 창에 WebView2 기반 PDF 뷰어 영역을 추가했다.
  - PDF 파일은 텍스트 박스가 아니라 WebView2 PDF 뷰어로 원본 레이아웃을 표시하도록 변경했다.
  - WebView2 표시가 실패하는 경우에만 기존 PdfPig 텍스트 파싱과 단순 PDF 문자열 추출을 fallback으로 사용한다.
- 문서 갱신:
  - `apps/windows/README.md`
  - `apps/windows/docs/explorer-shell.md`
  - `apps/windows/docs/local-sqlite-basics.md`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 업로드 DB 추적 기준 보정

- 상태: 완료
- 요청: 업로드 원본 파일은 GitHub에 올리지 않더라도 SQLite DB 기록은 올라가야 한다.
- 문제:
  - 실행 앱이 빌드 출력 폴더의 SQLite DB를 수정하면 `bin/` 무시 규칙 때문에 업로드 기록 DB가 커밋 대상에 잡히지 않을 수 있었다.
- 수정 내용:
  - 개발 실행 시 기본 SQLite DB 경로를 `apps/windows/src/FlowNote.Windows.App/Data/flownote.local.sqlite`로 해석하도록 변경했다.
  - 배포 실행처럼 소스 프로젝트 파일을 찾을 수 없는 경우에는 기존처럼 앱 실행 폴더의 `Data/flownote.local.sqlite`를 사용한다.
  - 업로드 파일은 DB가 있는 `Data/Files/Uploads/yyyy-MM-dd/` 아래에 복사하고, DB에는 앱 콘텐츠 루트 기준 상대 경로만 저장한다.
  - 보기 창은 상대 경로 파일을 실행 폴더와 개발 소스 폴더 양쪽 기준으로 찾아 열도록 보정했다.
- 문서 갱신:
  - `apps/windows/README.md`
  - `apps/windows/docs/explorer-shell.md`
  - `apps/windows/docs/local-sqlite-basics.md`

## 테스트 산출물 보존 규칙 반영

- 상태: 완료
- 요청: 기존에 테스트한 파일, 테스트 DB, 테스트 결과 로그, 테스트 내역 문서를 삭제하지 않도록 규칙을 명확히 남긴다.
- 반영 내용:
  - `AGENTS.md`에 테스트 파일, 테스트 SQLite DB, 테스트 결과 로그, 테스트 내역 문서는 사용자가 명시적으로 삭제를 지시하지 않는 한 삭제하지 않는다고 기록했다.
  - 불필요한 파일과 디렉토리를 정리할 때도 기존 테스트 산출물과 테스트 기록은 보존 대상이라고 기록했다.
  - 도면/문서 샘플 생성 과정에서 대체되었던 이전 공장 문서 샘플은 삭제하지 않고 `apps/windows/src/FlowNote.Windows.App/Data/Files/Samples/Factory-2026-06-23/` 하위 폴더에 다시 복원했다.
  - 복원된 공장 문서 샘플도 `jobhead / 반장 / 1234` 계정 기준으로 SQLite 테스트 DB에 다시 기록했다.
- 보존된 테스트 위치:
  - `tmp/functional-flow-test-2026-06-23/`
  - `tmp/photo-folder-app-db-test-2026-06-23/`
  - `tmp/factory-sample-generator-2026-06-23/`
  - `tmp/drawing-document-sample-generator-2026-06-23/`
  - `apps/windows/src/FlowNote.Windows.App/Data/Files/Samples/`
  - `apps/windows/src/FlowNote.Windows.App/Data/Files/Samples/Factory-2026-06-23/`
- 검증:
  - SQLite 테스트 DB에 총 27개 문서와 27개 코멘트 버전, `jobhead / 반장` 계정이 남아 있음을 확인했다.
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## Excel 행/열 그리드 보기 반영

- 상태: 완료
- 요청: Excel 파일은 텍스트가 아니라 Excel처럼 보여야 한다.
- 구현 내용:
  - 파일 보기 창에 Excel 전용 `DataGrid` 영역을 추가했다.
  - `.xlsx` 파일은 텍스트 박스가 아니라 첫 번째 시트의 행/열 그리드로 표시하도록 변경했다.
  - `inlineStr` 방식과 `sharedStrings.xml` 방식의 셀 문자열을 모두 읽도록 보강했다.
  - 셀 주소 기준으로 빈 칸을 유지해 A, B, C 열 위치가 어긋나지 않도록 했다.
- 테스트:
  - `tmp/document-preview-check-2026-06-23/`
  - TXT 샘플은 텍스트로 열리는 것을 확인했다.
  - Excel 샘플 `문서-설비점검표-프레스A.xlsx`는 `SpreadsheetPreview`가 `Visible`이고 4개 행이 로드되는 것을 확인했다.
- 문서 갱신:
  - `apps/windows/README.md`
  - `apps/windows/docs/explorer-shell.md`
  - `apps/windows/docs/local-sqlite-basics.md`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 알림함과 직전 버전 작성자 알림 반영

- 상태: 완료
- 요청: 문서에 코멘트가 달려 버전이 올라가면 알림함에 알림을 남기고, 알림 수신자는 최초 작성자가 아니라 직전 버전 작성자로 처리한다.
- 구현 내용:
  - SQLite에 `notifications` 테이블을 추가했다.
  - `NotificationRecord`, `NotificationService`를 추가했다.
  - 알림 목록 조회, 읽지 않은 알림 수 조회, 모두 읽음 처리를 구현했다.
  - `DocumentService.AddCommentVersion`에서 새 코멘트 버전을 저장할 때 직전 버전 작성자를 조회해 알림을 생성하도록 했다.
  - v2는 v1 작성자에게, v3는 v2 작성자에게 알림이 가는 구조로 처리했다.
  - WPF 메인 화면 상단에 `알림함` 버튼을 추가했다.
  - 읽지 않은 알림이 있으면 `알림함 (N)` 형식으로 표시하도록 했다.
  - `NotificationWindow`를 추가해 로그인 사용자에게 온 알림 목록과 모두 읽음 버튼을 제공했다.
- 문서 갱신:
  - `apps/windows/README.md`
  - `apps/windows/docs/explorer-shell.md`
  - `apps/windows/docs/local-sqlite-basics.md`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`
  - 스모크 테스트에서 v2 코멘트는 v1 작성자에게, v3 코멘트는 v2 작성자에게 알림이 생성되는 것을 확인했다.
  - 알림 모두 읽음 처리 후 읽지 않은 알림 수가 0개가 되는 것을 확인했다.

## 문서 갱신: 로컬 SQLite

- `apps/windows/README.md`에 로그인, 로컬 SQLite, 기본 계정, 검증 명령을 반영했다.
- `apps/windows/docs/local-sqlite-basics.md`에 로컬 SQLite 구조와 검증 범위를 기록했다.
- `apps/windows/docs/explorer-shell.md`에 로그인과 로컬 SQLite 바인딩 범위를 반영했다.

## 좌측 탐색기 트리와 기본 폴더

- 상태: 완료
- 범위: 탐색기 좌측 영역에서 로컬 SQLite 폴더 구조를 트리로 표시하고, 루트 아래 기본 폴더를 시스템 폴더로 보장했다.
- 구현 내용:
  - `Root` 아래 기본 폴더 `문서`, `인수인계`, `작업순서`, `사진` 자동 생성
  - 네 개 기본 폴더를 삭제 불가 시스템 폴더로 처리
  - 기존 로컬 DB에 같은 이름의 기본 폴더가 있으면 시스템 폴더로 보정
  - 문서 등록 기본 대상을 `문서` 폴더로 변경
  - 스모크 테스트에 기본 폴더 존재 여부와 삭제 차단 검증 추가
- 코드 위치:
  - `apps/windows/src/FlowNote.Windows.Core/Storage/FlowNoteLocalDatabase.cs`
  - `apps/windows/src/FlowNote.Windows.Core/Folders/FolderService.cs`
  - `apps/windows/src/FlowNote.Windows.App/MainWindow.xaml.cs`
  - `apps/windows/src/FlowNote.Windows.SmokeTests/Program.cs`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 불필요 디렉터리 정리

- 상태: 완료
- 범위: 현재 개발 방향에서 실제 사용 근거가 없는 루트 디렉터리와 설명을 제거했다.
- 삭제 대상:
  - `LocalDev/`
  - `assets/`
  - `packages/`
  - `scripts/`
- 유지 대상:
  - `apps/windows/`
  - `services/api/`
  - `docs/`
- 문서 갱신:
  - `AGENTS.md`의 Repository Layout에서 삭제 대상 항목 제거
  - `README.md`의 저장소 구조에서 삭제 대상 항목 제거
  - `.gitignore`에서 `LocalDev/launchagents/` 항목 제거
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## WPF 화면 UI 조정

- 상태: 완료
- 범위: 기능 검증용 샘플 화면에 가까웠던 WPF UI를 현장 문서 관리용 탐색기 화면 톤으로 조정했다.
- 구현 내용:
  - 상단 영역을 제품명, 업무 설명, 로그인 사용자, 주요 명령으로 정리
  - 좌측 폴더 트리 헤더와 여백 조정
  - 오른쪽 파일 목록의 헤더, 검색 영역, 컬럼명을 한글 업무 화면 기준으로 정리
  - 로그인 창과 파일 보기 창의 문구와 모서리, 여백을 메인 화면과 맞춤
  - 공통 WPF 리소스에 버튼, 텍스트박스, 데이터그리드 기본 스타일 추가
  - 상태 메시지와 업로드 후보 상태 문구 한글화
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 실제 테스트 파일 기반 기능 테스트

- 상태: 완료
- 범위: 실제 테스트 파일을 로컬에 생성하고 파일 등록, 목록 조회, Drag & Drop 후보 추가 흐름을 확인했다.
- 테스트 위치:
  - `tmp/functional-flow-test-2026-06-23/`
- 남긴 테스트 산출물:
  - `tmp/functional-flow-test-2026-06-23/files/현장점검-2026-06-23.txt`
  - `tmp/functional-flow-test-2026-06-23/files/인수인계-주간.txt`
  - `tmp/functional-flow-test-2026-06-23/files/작업순서-라인A-조립검사.xlsx`
  - `tmp/functional-flow-test-2026-06-23/files/라인A-설비사진-2026-06-23.jpg`
  - `tmp/functional-flow-test-2026-06-23/flownote-functional-flow.sqlite`
  - `tmp/functional-flow-test-2026-06-23/functional-flow-result.md`
  - `tmp/functional-flow-test-2026-06-23/FunctionalFlowCheck.cs`
  - `tmp/functional-flow-test-2026-06-23/FunctionalFlowCheck.csproj`
- 테스트 계정:
  - ID: `jobhead`
  - 이름: `반장`
  - 비밀번호: `1234`
- SQLite 기록:
  - `user_accounts`에 `jobhead / 반장` 계정 기록
  - `documents`에 `반장` 작성자로 일반 문서, 인수인계 문서, 작업순서 문서, 사진 문서 기록
  - `document_folders`에 `/인수인계/2026-06-23`, `/사진/2026-06-23` 날짜 폴더 기록
- 확인한 흐름:
  - `jobhead / 1234` 로그인 성공
  - 실제 파일 4개 생성
  - 파일 등록 후 전체 문서 목록 조회 성공
  - 인수인계 파일이 날짜 하위 폴더에 배치됨
  - 사진 파일이 날짜 하위 폴더에 배치됨
  - 작업순서 파일 제목이 파일명 기반으로 자동 생성됨
  - Drag & Drop 후보가 파일 목록 최상단에 추가되고 사용자 이름 `반장`과 로컬 경로가 유지됨
- 결과:
  - 기능 테스트 통과
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 사진 폴더 실제 앱 DB 테스트 보정

- 상태: 완료
- 문제: 이전 사진 폴더 테스트는 별도 `tmp` SQLite DB에만 기록되어 실제 앱에서 여는 `%LOCALAPPDATA%\FlowNote\flownote.local.sqlite`에는 사진 등록 내역이 보이지 않았다.
- 보정 내용:
  - 실제 앱 SQLite DB `C:\Users\truds\AppData\Local\FlowNote\flownote.local.sqlite`에 테스트 기록을 남겼다.
  - 테스트 계정 `jobhead / 반장 / 1234`를 실제 앱 DB에 기록했다.
  - 테스트 사진 파일 `tmp/photo-folder-app-db-test-2026-06-23/files/사진폴더-설비점검-2026-06-23.jpg`를 생성하고 삭제하지 않았다.
  - 사진 루트 폴더 `/사진` 아래 날짜 폴더 `/사진/2026-06-23`를 생성했다.
  - 사진 문서 `사진폴더-설비점검-2026-06-23.jpg`를 작성자 `반장`으로 등록했다.
  - 테스트 결과 로그 `tmp/photo-folder-app-db-test-2026-06-23/photo-folder-app-db-result.md`를 남겼다.
- 확인 방법:
  - 앱에서 `admin / 1234` 또는 `jobhead / 1234`로 로그인한다.
  - 왼쪽 트리에서 `사진` 폴더를 더블클릭한다.
  - `2026-06-23` 하위 폴더를 원클릭한다.
  - 오른쪽 파일 목록에서 `사진폴더-설비점검-2026-06-23.jpg` 항목을 확인한다.

## SQLite DB 위치를 빌드 출력 경로로 변경

- 상태: 완료
- 범위: WPF 앱의 기본 SQLite DB 위치를 `%LOCALAPPDATA%`에서 앱 빌드 출력 폴더로 변경했다.
- 변경 내용:
  - `FlowNoteLocalDatabase.DefaultDatabasePath`를 `AppContext.BaseDirectory\Data\flownote.local.sqlite` 기준으로 변경
  - `apps/windows/src/FlowNote.Windows.App/Data/flownote.local.sqlite`를 소스 시드 DB로 추가
  - WPF 앱 빌드 시 소스 시드 DB를 출력 폴더 `Data\flownote.local.sqlite`로 복사하도록 `.csproj`에 설정 추가
  - `.gitignore`에서 해당 소스 시드 SQLite 파일은 Git 추적 대상이 되도록 예외 추가
  - Windows 앱 README와 로컬 SQLite 문서의 DB 경로 설명 수정
- 시드 DB 확인:
  - 소스 시드 DB `apps/windows/src/FlowNote.Windows.App/Data/flownote.local.sqlite`에 `jobhead / 반장` 계정 기록 확인
  - 소스 시드 DB에 `/사진/2026-06-23` 폴더와 `사진폴더-설비점검-2026-06-23.jpg` 문서 기록 확인
  - 빌드 출력 DB `apps/windows/src/FlowNote.Windows.App/bin/Debug/net10.0-windows/Data/flownote.local.sqlite`에 같은 기록이 복사된 것을 확인
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 파일 보기 코멘트와 문서 버전 증가

- 상태: 완료
- 범위: 파일 보기 창에서 코멘트를 저장하고 문서 버전을 올리는 기본 흐름을 구현했다.
- 구현 내용:
  - `documents` 테이블에 `updated_at`, `local_path`, `version_no`, `latest_comment` 컬럼 추가
  - `document_versions` 테이블 추가
  - 문서 등록 시 원본 `v1` 버전 기록 생성
  - 파일 보기 창에 코멘트 입력과 저장 버튼 추가
  - 코멘트 저장 시 새 버전을 추가하고 문서의 최신 버전 번호와 최신 코멘트를 갱신
  - 파일 보기 창에 버전 이력 목록 표시
  - 사진 파일은 이미지 미리보기 아래에 코멘트 영역과 버전 이력을 표시
  - 상대 로컬 경로는 앱 빌드 출력 폴더 기준으로 해석
  - 스모크 테스트에 코멘트 저장 후 버전 증가 검증 추가
- 시드 DB 확인:
  - 소스 시드 DB와 빌드 출력 DB 모두에서 `사진폴더-설비점검-2026-06-23.jpg`가 `v2`로 저장됨
  - 최신 코멘트 `설비 점검 사진입니다. 이상 없음.` 확인
  - 버전 이력 수 2개 확인
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 폴더 트리 더블클릭 펼침 수정

- 상태: 완료
- 문제: 왼쪽 폴더를 더블클릭해도 하위 폴더가 펼쳐지지 않았다.
- 원인: WPF `TreeViewItem` 기본 더블클릭 동작과 앱의 `MouseDoubleClick` 토글 처리가 연속으로 실행되어 펼침 상태가 다시 원상복구될 수 있었다.
- 수정 내용:
  - TreeView 이벤트를 `MouseDoubleClick`에서 `PreviewMouseDoubleClick`으로 변경했다.
  - 기본 더블클릭 처리 전에 앱에서 한 번만 펼침/접힘을 처리하도록 했다.
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 공장 문서 샘플 12개 등록

- 상태: 완료
- 범위: 공장에서 사용할 법한 문서 샘플을 실제 파일로 만들고 SQLite 시드 DB에 등록했다.
- 샘플 위치:
  - `apps/windows/src/FlowNote.Windows.App/Data/Files/Samples/`
- 등록 계정:
  - `jobhead / 반장 / 1234`
- 등록 파일:
  - `안전수칙-용접작업.txt`
  - `설비점검-프레스A.xlsx`
  - `작업표준서-혼합공정.pdf`
  - `품질체크-포장라인.xlsx`
  - `교육자료-지게차안전.pdf`
  - `인수인계-주간조.txt`
  - `인수인계-야간요약.pdf`
  - `작업순서-라인A-조립검사.xlsx`
  - `작업순서-세척공정.txt`
  - `작업순서-포장전검사.pdf`
  - `사진-프레스A-점검.jpg`
  - `사진-포장라인-라벨위치.png`
- 파일 종류:
  - Text 3개
  - PDF 4개
  - Spreadsheet 3개
  - Image 2개
- 구현 내용:
  - `txt`, `pdf`, `xlsx`, `jpg`, `png` 파일에 실제 샘플 내용을 채움
  - `xlsx` 미리보기를 위해 첫 번째 시트의 셀 텍스트를 읽어 표시하도록 파일 보기 창 보강
  - 샘플 `pdf` 미리보기를 위해 PDF 내 간단 텍스트를 추출해 표시하도록 파일 보기 창 보강
  - 각 문서에 코멘트 버전 `v2`를 추가해 버전 이력 확인 가능
  - `.gitignore`에서 샘플 파일 경로는 추적되도록 예외 추가
- DB 확인:
  - 소스 시드 DB 문서 수: 12개
  - 빌드 출력 DB 문서 수: 12개
  - `/인수인계/2026-06-23`, `/사진/2026-06-23` 날짜 폴더 생성 확인
  - `document_versions`의 코멘트 버전 12개 확인
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 파일 보기 누적 코멘트 표시 방식 변경

- 상태: 완료
- 문제: 코멘트를 별도 버전 이력 박스에 표시하면 문서 내용과 코멘트를 한 번에 보기 어렵다.
- 수정 내용:
  - 파일 보기 창에서 `버전 이력` 별도 목록을 제거했다.
  - 문서 내용 아래에 `누적 코멘트` 영역을 추가했다.
  - 코멘트를 저장하면 누적 코멘트 영역에 시간, 작성자, 버전과 함께 계속 추가되도록 변경했다.
  - 사진 문서는 이미지 미리보기 아래에 누적 코멘트와 새 코멘트 입력 영역을 표시하도록 변경했다.
  - 코멘트 저장 시 문서 버전 증가는 기존처럼 유지한다.
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 누적 코멘트 최신순 표시

- 상태: 완료
- 범위: 파일 보기 창의 누적 코멘트 표시 순서를 최신 버전이 위로 오도록 변경했다.
- 변경 내용:
  - 내부 버전 증가는 기존처럼 유지한다.
  - 화면의 누적 코멘트 목록은 `v3`, `v2`, `v1`처럼 최신순으로 표시한다.
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 로그인 창 표시 문제 수정

- 상태: 완료
- 문제: 로그인 창 높이와 내부 배치가 부족해 비밀번호 입력 아래의 로그인 버튼이 화면에 보이지 않았다.
- 수정 내용:
  - `LoginWindow.xaml`의 창 높이와 최소 높이를 더 여유 있게 늘렸다.
  - 로그인 폼 내부를 고정 행 `Grid` 구조로 변경해 버튼 영역이 잘리지 않도록 했다.
  - 로그인 버튼 여백과 높이를 조정했다.
  - 비밀번호 입력칸의 세로 정렬을 맞췄다.
- 실제 확인:
  - `tmp/login-window-verify-fixed.png`로 실행 중인 `FlowNote 로그인` 창을 캡처했다.
  - 캡처 결과 로그인 버튼이 화면에 표시되는 것을 확인했다.
- 검증:
  - 최초 빌드는 실행 중인 `FlowNote.Windows.App` 프로세스가 exe를 잠가 실패했다.
  - 실행 중인 앱 프로세스를 종료한 뒤 재빌드했다.
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 탐색기 트리 아이콘 UI 반영

- 상태: 완료
- 범위: 왼쪽 폴더 트리를 Windows 탐색기와 비슷하게 아이콘과 트리 구조 중심으로 다시 표현했다.
- 수정 내용:
  - `ExplorerFolder`에 폴더 아이콘 표시용 glyph 속성 추가
  - TreeView 항목을 기본 펼침 상태로 설정
  - TreeView 항목 높이, 여백, 스크롤 설정 조정
  - 폴더명 앞에 `Segoe MDL2 Assets` 기반 폴더 아이콘 표시
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 탐색기 트리 선택과 더블클릭 동작 반영

- 상태: 완료
- 범위: 왼쪽 폴더 트리를 Windows 탐색기처럼 원클릭 선택과 더블클릭 열기 흐름으로 조정했다.
- 구현 내용:
  - 폴더 원클릭 선택 시 오른쪽 문서 목록을 해당 폴더의 파일 목록으로 갱신
  - 폴더 더블클릭 시 TreeView 항목을 펼치거나 접도록 처리
  - 루트 폴더는 초기 펼침 상태로 표시
  - 오른쪽 문서 목록 제목에 선택 폴더명과 표시 파일 수 반영
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 문서 갱신: 기본 폴더 트리

- `apps/windows/README.md`에 루트 아래 기본 폴더 4개와 삭제 불가 정책을 반영했다.
- `apps/windows/docs/local-sqlite-basics.md`에 기본 폴더 자동 생성과 시스템 폴더 정책을 반영했다.
- `apps/windows/docs/explorer-shell.md`에 좌측 트리의 기본 폴더 구조를 반영했다.

## 오른쪽 파일 목록과 Drag & Drop 업로드 후보

- 상태: 완료
- 범위: 탐색기 오른쪽 작업 영역을 파일 목록 기본 화면으로 유지하고, 파일 목록에 Drag & Drop 업로드 후보를 추가하도록 구현했다.
- 구현 내용:
  - 오른쪽 작업 영역에 파일 목록 표시
  - 파일 목록에 파일명, 제목, 유형, 상태, 버전, 사용자, 버전 생성 시간 표시
  - 파일 목록 영역에 Drag & Drop 이벤트 연결
  - 드래그 진입, 이탈, 오버, 드롭 이벤트 처리
  - 드롭된 파일을 파일 목록의 업로드 후보 항목으로 추가
  - 업로드 후보에 로그인 사용자 이름과 후보 생성 시간 표시
  - 파일 목록 더블 클릭 시 별도 파일 보기 창 표시
  - 로컬 경로가 있는 작은 텍스트 파일은 보기 창에서 내용 표시
  - 업로드 후보 모델 `UploadCandidate` 추가
  - 파일 보기 창 `DocumentViewWindow` 추가
  - 스모크 테스트에 업로드 후보가 파일 목록에 추가되는지 검증 추가
- 코드 위치:
  - `apps/windows/src/FlowNote.Windows.App/MainWindow.xaml`
  - `apps/windows/src/FlowNote.Windows.App/MainWindow.xaml.cs`
  - `apps/windows/src/FlowNote.Windows.App/DocumentViewWindow.xaml`
  - `apps/windows/src/FlowNote.Windows.App/DocumentViewWindow.xaml.cs`
  - `apps/windows/src/FlowNote.Windows.Core/Explorer/UploadCandidate.cs`
  - `apps/windows/src/FlowNote.Windows.Core/Explorer/ExplorerWorkspace.cs`
  - `apps/windows/src/FlowNote.Windows.SmokeTests/Program.cs`
- 검증:
  - 최초 병렬 검증 중 `VBCSCompiler` 파일 잠금으로 WPF 빌드가 실패했다.
  - `dotnet build-server shutdown`으로 컴파일 서버 잠금을 해제한 뒤 재시도했다.
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`

## 문서 갱신: 파일 목록과 Drag & Drop 업로드 후보

- `apps/windows/README.md`에 오른쪽 기본 화면이 파일 목록이라는 기준과 더블 클릭 보기 흐름을 반영했다.
- `apps/windows/docs/explorer-shell.md`에 파일 목록, Drag & Drop 후보 추가, 더블 클릭 보기 창 흐름을 반영했다.
- `apps/windows/docs/local-sqlite-basics.md`에 업로드 후보가 파일 목록에 추가되는 검증을 반영했다.
- 문서 갱신 후 `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`를 재실행했고 경고 0개, 오류 0개로 통과했다.
- 문서 갱신 후 `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`를 재실행했고 `FlowNote Windows smoke tests passed.` 결과를 확인했다.

## 기본 폴더별 등록 규칙 반영

- 상태: 완료
- 범위: `인수인계`, `작업순서`, `사진` 기본 폴더에 파일 등록 규칙을 추가했다.
- 구현 내용:
  - 기본 폴더 이름을 `FlowNoteLocalDatabase` 상수로 정리
  - `FolderService`에 기본 시스템 폴더 조회와 하위 폴더 없으면 생성 기능 추가
  - `DocumentPlacementService` 추가
  - `인수인계` 선택 시 `yyyy-MM-dd` 날짜 하위 폴더 생성 후 문서 배치
  - `사진` 선택 시 `yyyy-MM-dd` 날짜 하위 폴더 생성 후 문서 배치
  - `작업순서` 선택 시 파일명에서 확장자를 제외한 값을 작업 제목으로 자동 생성
  - 루트 선택 또는 폴더 미선택 시 기본 대상은 `문서` 폴더로 처리
  - WPF 트리 선택 상태를 문서 등록과 Drag & Drop 후보 추가 흐름에 연결
  - 스모크 테스트에 날짜 하위 폴더 생성과 작업 제목 자동 생성 검증 추가
- 문서 갱신:
  - `apps/windows/README.md`
  - `apps/windows/docs/explorer-shell.md`
  - `apps/windows/docs/local-sqlite-basics.md`
- 검증:
  - `dotnet build .\apps\windows\src\FlowNote.Windows.App\FlowNote.Windows.App.csproj`
  - 결과: 경고 0개, 오류 0개
  - `dotnet run --project .\apps\windows\src\FlowNote.Windows.SmokeTests\FlowNote.Windows.SmokeTests.csproj`
  - 결과: `FlowNote Windows smoke tests passed.`
