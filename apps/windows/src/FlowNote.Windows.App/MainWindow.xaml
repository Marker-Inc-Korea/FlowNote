<Window x:Class="FlowNote.Windows.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FlowNote"
        Width="1200"
        Height="760"
        MinWidth="980"
        MinHeight="620"
        Background="{StaticResource WindowBackgroundBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64" />
            <RowDefinition Height="*" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="0,0,0,1">
            <DockPanel Margin="18,0">
                <StackPanel DockPanel.Dock="Left"
                            VerticalAlignment="Center">
                    <TextBlock Text="FlowNote"
                               FontSize="21"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryTextBrush}" />
                    <TextBlock Text="현장 문서와 작업 지식 관리"
                               Margin="0,2,0,0"
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}" />
                </StackPanel>
                <StackPanel DockPanel.Dock="Right"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <TextBlock x:Name="SignedInUserTextBlock"
                               Margin="0,0,18,0"
                               FontSize="13"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               VerticalAlignment="Center" />
                    <Button x:Name="NotificationButton"
                            Width="104"
                            Height="34"
                            Margin="0,0,8,0"
                            Content="알림함"
                            Click="NotificationButton_Click" />
                    <Button Width="92"
                            Height="34"
                            Margin="0,0,8,0"
                            Content="새 폴더"
                            Click="NewFolderButton_Click" />
                    <Button Width="104"
                            Height="34"
                            Margin="0,0,8,0"
                            Content="문서 등록"
                            Click="RegisterDocumentButton_Click" />
                    <Button Width="104"
                            Height="34"
                            Content="파일 업로드"
                            Click="UploadFileButton_Click" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="282" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource PanelBackgroundBrush}"
                    BorderBrush="{StaticResource PanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                <DockPanel>
                    <Border DockPanel.Dock="Top"
                            Height="48"
                            Padding="14,0"
                            Background="{StaticResource PanelMutedBrush}"
                            BorderBrush="{StaticResource PanelBorderBrush}"
                            BorderThickness="0,0,0,1">
                        <DockPanel>
                            <TextBlock Text="폴더"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryTextBrush}"
                                       VerticalAlignment="Center" />
                            <TextBlock DockPanel.Dock="Right"
                                       Text="문서 구조"
                                       FontSize="12"
                                       Foreground="{StaticResource SecondaryTextBrush}"
                                       VerticalAlignment="Center" />
                        </DockPanel>
                    </Border>
                    <TreeView ItemsSource="{Binding Folders}"
                              SelectedItemChanged="FolderTree_SelectedItemChanged"
                              PreviewMouseDoubleClick="FolderTree_PreviewMouseDoubleClick"
                              Margin="10,8,8,10"
                              BorderThickness="0"
                              Background="{StaticResource PanelBackgroundBrush}"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <Grid Height="24" VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="22" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="{Binding IconGlyph}"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="15"
                                               Foreground="#D79B22"
                                               VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Name}"
                                               Grid.Column="1"
                                               Foreground="{StaticResource PrimaryTextBrush}"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </DockPanel>
            </Border>

            <Grid Grid.Column="2"
                  x:Name="FileListDropZone"
                  AllowDrop="True"
                  DragEnter="FileListDropZone_DragEnter"
                  DragLeave="FileListDropZone_DragLeave"
                  DragOver="FileListDropZone_DragOver"
                  Drop="FileListDropZone_Drop">
                <Grid.RowDefinitions>
                    <RowDefinition Height="48" />
                    <RowDefinition Height="44" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="{StaticResource PanelBackgroundBrush}"
                        BorderBrush="{StaticResource PanelBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="14,0">
                    <DockPanel>
                        <TextBlock Text="검색"
                                   Margin="0,0,10,0"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource PrimaryTextBrush}"
                                   VerticalAlignment="Center" />
                        <TextBox Width="360"
                                 Height="30"
                                 VerticalContentAlignment="Center" />
                        <TextBlock DockPanel.Dock="Right"
                                   Text="파일 목록"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   VerticalAlignment="Center" />
                    </DockPanel>
                </Border>

                <Border Grid.Row="1"
                        Margin="0,8,0,0"
                        Background="{StaticResource PanelMutedBrush}"
                        BorderBrush="{StaticResource PanelBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="14,0">
                    <DockPanel>
                        <TextBlock x:Name="DocumentListTitleTextBlock"
                                   Text="문서 목록"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource PrimaryTextBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock x:Name="DocumentListHintTextBlock"
                                   DockPanel.Dock="Right"
                                   Text="파일명, 제목, 상태, 버전, 사용자, 생성시간"
                                   FontSize="12"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   VerticalAlignment="Center" />
                    </DockPanel>
                </Border>

                <Border Grid.Row="2"
                        Margin="0,8,0,0"
                        x:Name="FileListPanel"
                        Background="{StaticResource PanelBackgroundBrush}"
                        BorderBrush="{StaticResource PanelBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4">
                    <DataGrid ItemsSource="{Binding Documents}"
                              MouseDoubleClick="DocumentGrid_MouseDoubleClick"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              BorderThickness="0"
                              RowHeight="39"
                              ColumnHeaderHeight="34">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="파일명" Binding="{Binding FileName}" Width="*" />
                            <DataGridTextColumn Header="제목" Binding="{Binding Title}" Width="180" />
                            <DataGridTextColumn Header="유형" Binding="{Binding DocumentType}" Width="80" />
                            <DataGridTextColumn Header="상태" Binding="{Binding Status}" Width="110" />
                            <DataGridTextColumn Header="버전" Binding="{Binding VersionLabel}" Width="76" />
                            <DataGridTextColumn Header="사용자" Binding="{Binding UpdatedBy}" Width="130" />
                            <DataGridTextColumn Header="생성시간" Binding="{Binding UpdatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="152" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Row="2"
                Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="0,1,0,0">
            <TextBlock Margin="16,0"
                       Text="{Binding StatusText}"
                       FontSize="12"
                       Foreground="{StaticResource SecondaryTextBrush}"
                       VerticalAlignment="Center" />
        </Border>
    </Grid>
</Window>
