<Window x:Class="FlowNote.Windows.App.FileWatchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Watch"
        Width="980"
        Height="640"
        MinWidth="820"
        MinHeight="540"
        Background="{StaticResource WindowBackgroundBrush}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="44" />
        </Grid.RowDefinitions>

        <Border Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Watch Folder"
                           Margin="0,0,10,0"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryTextBrush}"
                           VerticalAlignment="Center" />
                <TextBox x:Name="WatchFolderTextBox"
                         Grid.Column="1"
                         Height="30"
                         VerticalContentAlignment="Center" />
                <Button Grid.Column="2"
                        Width="84"
                        Height="32"
                        Margin="8,0,0,0"
                        Content="Browse"
                        Click="BrowseButton_Click" />
                <Button x:Name="StartWatchButton"
                        Grid.Column="3"
                        Width="84"
                        Height="32"
                        Margin="8,0,0,0"
                        Content="Start"
                        Click="StartWatchButton_Click" />
                <Button x:Name="StopWatchButton"
                        Grid.Column="4"
                        Width="84"
                        Height="32"
                        Margin="8,0,0,0"
                        Content="Stop"
                        Click="StopWatchButton_Click" />
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
            <DataGrid x:Name="CandidateGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      RowHeight="42"
                      ColumnHeaderHeight="34"
                      SelectionChanged="CandidateGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Detected" Binding="{Binding DetectedAt, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="150" />
                    <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="*" />
                    <DataGridTextColumn Header="Matched Document" Binding="{Binding DocumentTitle}" Width="220" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
                    <DataGridTextColumn Header="Size" Binding="{Binding SizeBytes}" Width="90" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="4"
                Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Target Document"
                           Margin="0,0,10,0"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryTextBrush}"
                           VerticalAlignment="Center" />
                <ComboBox x:Name="DocumentComboBox"
                          Grid.Column="1"
                          Height="30"
                          DisplayMemberPath="DisplayText"
                          SelectedValuePath="DocumentId" />
                <TextBlock Grid.Column="2"
                           Text="Version"
                           Margin="12,0,8,0"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryTextBrush}"
                           VerticalAlignment="Center" />
                <TextBox x:Name="VersionLabelTextBox"
                         Grid.Column="3"
                         Height="30"
                         VerticalContentAlignment="Center" />

                <TextBlock Grid.Row="2"
                           Text="Change Reason"
                           Margin="0,0,10,0"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryTextBrush}"
                           VerticalAlignment="Center" />
                <TextBox x:Name="ChangeReasonTextBox"
                         Grid.Row="2"
                         Grid.Column="1"
                         Grid.ColumnSpan="3"
                         Height="30"
                         VerticalContentAlignment="Center" />
            </Grid>
        </Border>

        <DockPanel Grid.Row="5"
                   VerticalAlignment="Bottom">
            <TextBlock x:Name="StatusTextBlock"
                       DockPanel.Dock="Left"
                       Foreground="{StaticResource SecondaryTextBrush}"
                       VerticalAlignment="Center" />
            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Width="84"
                        Height="34"
                        Margin="0,0,8,0"
                        Content="Refresh"
                        Click="RefreshButton_Click" />
                <Button Width="84"
                        Height="34"
                        Margin="0,0,8,0"
                        Content="Confirm"
                        Click="ConfirmButton_Click" />
                <Button Width="84"
                        Height="34"
                        Margin="0,0,8,0"
                        Content="Ignore"
                        Click="IgnoreButton_Click" />
                <Button Width="84"
                        Height="34"
                        Content="Close"
                        Click="CloseButton_Click" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
