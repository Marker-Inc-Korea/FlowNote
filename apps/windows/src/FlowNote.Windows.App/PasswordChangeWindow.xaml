<Window x:Class="FlowNote.Windows.App.PasswordChangeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="최초 비밀번호 변경"
        Width="430"
        Height="330"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource WindowBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="비밀번호 변경이 필요합니다"
                   FontSize="18"
                   FontWeight="SemiBold" />
        <TextBlock Grid.Row="1"
                   Margin="0,8,0,14"
                   Text="임시 비밀번호로 로그인했습니다. 8자 이상의 새 비밀번호로 변경한 뒤 다시 로그인하세요."
                   TextWrapping="Wrap"
                   Foreground="{StaticResource SecondaryTextBrush}" />
        <TextBlock Grid.Row="2" Text="새 비밀번호" />
        <PasswordBox Grid.Row="3" x:Name="NewPasswordBox" Height="32" Margin="0,5,0,10" />
        <StackPanel Grid.Row="4">
            <TextBlock Text="새 비밀번호 확인" />
            <PasswordBox x:Name="ConfirmPasswordBox" Height="32" Margin="0,5,0,0" />
            <TextBlock x:Name="ErrorTextBlock" Margin="0,10,0,0" Foreground="#B42318" TextWrapping="Wrap" />
        </StackPanel>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="82" Height="34" Margin="0,0,8,0" Content="취소" IsCancel="True" />
            <Button x:Name="ChangeButton" Width="112" Height="34" Content="변경" Click="ChangeButton_Click" />
        </StackPanel>
    </Grid>
</Window>
