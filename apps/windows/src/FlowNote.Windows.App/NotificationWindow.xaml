<Window x:Class="FlowNote.Windows.App.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notifications"
        Width="760"
        Height="520"
        MinWidth="660"
        MinHeight="420"
        Background="{StaticResource WindowBackgroundBrush}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
            <RowDefinition Height="44" />
        </Grid.RowDefinitions>

        <Border Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="14">
            <StackPanel>
                <TextBlock Text="Notifications"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryTextBrush}" />
                <TextBlock x:Name="SummaryTextBlock"
                           Margin="0,6,0,0"
                           Foreground="{StaticResource SecondaryTextBrush}" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
            <DataGrid x:Name="NotificationGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      RowHeight="42"
                      ColumnHeaderHeight="34">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Read" Binding="{Binding IsRead}" Width="58" />
                    <DataGridTextColumn Header="Type" Binding="{Binding TypeLabel}" Width="118" />
                    <DataGridTextColumn Header="Time" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="138" />
                    <DataGridTextColumn Header="Target" Binding="{Binding DisplayTitle}" Width="170" />
                    <DataGridTextColumn Header="Actor" Binding="{Binding ActorName}" Width="96" />
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">
            <Button Width="116"
                    Height="34"
                    Margin="0,0,8,0"
                    Content="Read Selected"
                    Click="MarkSelectedReadButton_Click" />
            <Button Width="104"
                    Height="34"
                    Margin="0,0,8,0"
                    Content="Read All"
                    Click="MarkAllReadButton_Click" />
            <Button Width="84"
                    Height="34"
                    Content="Close"
                    Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>
