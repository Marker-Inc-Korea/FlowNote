# Legacy Windows Code

This directory preserves the previous Windows/.NET code and build artifacts that existed before the FlowNote direction was reset.

It is kept for reference only. New Windows app work should happen under `apps/windows/src`.
