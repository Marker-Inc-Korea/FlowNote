# Legacy React/Vite Web UI

This directory preserves the previous React/Vite web UI implementation.

It is kept for reference only. New frontend work should happen in the WPF or Avalonia native client, not in a standalone web SPA.
