# Packages

Reusable code shared across apps lives here.

- `shared/`: Cross-platform business logic, types, constants, and utilities
- `ui/`: Shared UI resources or design-system code when the chosen stack supports it
