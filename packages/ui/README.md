# UI Package

WPF/Avalonia 클라이언트에서 참고할 공통 UI 자원 후보를 보관하는 영역이다. 독립 Web UI용 패키지로 사용하지 않는다.

## 예상 내용

- 문서 유형 아이콘
- 파일 상태와 버전 상태 표시 규칙
- 알림, 경고, 주의 사항 표시용 디자인 토큰
- 현장/관리자 클라이언트에서 공유 가능한 UI 규칙

실제 UI 자원은 WPF/Avalonia 클라이언트에서 재사용할 필요가 생길 때 구성한다.
