# Database

`init.sql` is the existing MySQL initialization script preserved from the previous API implementation.

The active development database is SQLite first. Use this file only as a reference for the Python/FastAPI migration. If the site grows beyond SQLite, PostgreSQL is the preferred upgrade path.
