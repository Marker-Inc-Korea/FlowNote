# Legacy Node API

This directory preserves the previous Node.js API implementation.

It is kept for reference only. New backend work should happen in the Python FastAPI structure under `services/api/app`.
